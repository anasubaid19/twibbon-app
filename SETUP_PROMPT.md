Baca file PRD.md di root project ini.

Baca juga folder `backend/` dan `frontend/` — pahami logika existing-nya.

Kemudian:

1. Hapus `backend/` dan `frontend/`
2. Tulis ulang project sebagai TanStack Start app di root dengan stack:
   - TanStack Start (React 19 + TanStack Router + server functions)
   - PostgreSQL + Drizzle ORM
   - Better Auth (email/password, HTTP-only session cookies)
   - shadcn/ui + Tailwind CSS v4
   - Sharp (image processing)
   - Biome (linter/formatter)
   - Fabric.js (canvas area drawing)

Gunakan skill berikut:
- **ponytail** — kode seminimal mungkin, jangan over-engineer. YAGNI.
- **apple-design** — springs, 1:1 pointer tracking, backdrop-filter translucency, system font
- **find-docs** — cek dokumentasi Better Auth, TanStack Start, Fabric.js jika ragu API

Desain UI pakai Bahasa Indonesia, brand name "OpenFrame".

Semua di satu project root. Tidak ada folder `backend/` atau `frontend/` terpisah.
